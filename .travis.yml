language: node_js
node_js:
  - '12'
script: yarn install && yarn test:nowatch && yarn build-storybook
deploy:
  provider: pages
  edge: true
  skip_cleanup: true
  github_token: $github_token
  local_dir: storybook-static
  on:
    branch: main
  commit_message: "Deploy github pages [ci skip]"
